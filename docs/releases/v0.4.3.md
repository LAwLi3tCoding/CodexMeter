# CodexMeter v0.4.3

CodexMeter 0.4.3 makes historical USD estimates more representative of actual model usage.

## Changes

- Weight each displayed period by the model-token mix observed in local thread metadata instead of pricing all tokens as the current configured model.
- Show an exclamation-mark estimate label whose hover text explains the observed-model weighting, workload mix, and attribution limits.
